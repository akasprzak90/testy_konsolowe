if {[catch {add_object abc}]} {
	puts "Znaleziono blad"
} else {
	puts "Brak bledow"
}
if {[catch {add_object d -below abc}]} {
	puts "Znaleziono blad"
} else {
	puts "Brak bledow"
}
display_object


